# railway.toml para api-gateway
[build]
dockerfilePath = "api-gateway/Dockerfile"

[deploy]
startCommand = ""
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "api-gateway"
